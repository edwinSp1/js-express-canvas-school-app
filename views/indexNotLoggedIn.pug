html
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width,minimum-scale=1")
        title Land
        // the form awesome library is used to add icons to our form
        link(rel="stylesheet", href="https://use.fontawesome.com/releases/v5.7.1/css/all.css")
        // include the stylesheet file
        link(href="stylesheets/landingpage.css", rel="stylesheet", type="text/css")
    body
        div(class='navbar')
            h1(style='display:inline') Mercury
            a(href='/adduser' style='float:right') Create Account
            a(href='/login' style='float:right') Login 
        div(id='main-container')
            h1(id='statement') For The Next Generation of Schools.
            h1(id='teacher-apply') Are you a teacher? Email echen5503@student.pps.net with your desired username.
            ul(id='announcements') 
                li Debut of the site scheduled for October 20th.
                li Admins will be red.
                li I will be blue.
            img(src='../public/images/assignments.png')
        
        div(class='background-image')
        script(src='javascripts/landingpage.js') 
    